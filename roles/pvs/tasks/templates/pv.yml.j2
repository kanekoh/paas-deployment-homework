  apiVersion: v1
  kind: PersistentVolume
  metadata:
    name: "{{PV_NAME}}"
  spec:
    capacity:
      storage: 5Gi
    accessModes:
      - "{{PV_ACCESS_MODE}}"
    persistentVolumeReclaimPolicy: Recycle
    nfs:
      path: /srv/nfs
      server: support1.fca3.internal